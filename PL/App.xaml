<Application x:Class="PL.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:PL"           
             
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <!--<DataTemplate x:Key="tmplProduct" DataType="bo.Product" >
            <TextBlock Margin="-5,-1" Text="{Binding Name}" TextAlignment="Center" Padding="2"/>
        </DataTemplate>

        <ObjectDataProvider x:Key="Categories" MethodName="GetValues"
                            ObjectType="{x:Type sys:Enum}">
            <ObjectDataProvider.MethodParameters>
                <x:Type TypeName="bo:Category"/>
            </ObjectDataProvider.MethodParameters>
        </ObjectDataProvider>
        <ObjectDataProvider x:Key="OrderStatuses" MethodName="GetValues">

            <ObjectDataProvider.MethodParameters>
              
            </ObjectDataProvider.MethodParameters>
        </ObjectDataProvider>-->
    </Application.Resources>    
</Application>
